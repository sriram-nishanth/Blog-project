<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/style/layout.css" rel="stylesheet"/>
</head>
<body class="d-flex justify-content-center align-items-center vh-100"> 

    <div id="blur" class="p-5 w-50 shadow rounded">
        <h1 class="text-center mb-4">Edit Post</h1>
        <form action="/update/<%= post.id %>" method="POST">
            
            <input class="form-control mb-3 text-center" type="text" name="title" value="<%= post.title %>" required>
            <textarea class="form-control mb-3 text-center" name="content" rows="5" required><%= post.content %></textarea>
            
            <div class="text-center">
                <button class="btn btn-primary px-4" type="submit">Save Changes</button>
                <a href="/" class="btn btn-secondary ms-2">Cancel</a>
            </div>
        </form>
    </div>

</body>
</html>
